import 'package:flutter/material.dart';

class ProductAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Product analysis"),);
  }
}
