import 'package:flutter/material.dart';

class ReturnedOrderAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Returned order analysis"),);
  }
}
