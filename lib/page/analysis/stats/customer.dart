import 'package:flutter/material.dart';

class CustomerAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Customer analysis"),);
  }
}
